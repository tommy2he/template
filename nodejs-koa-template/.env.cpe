# .env.cpe - 更新配置
# ========== CPE基本配置 ==========
CPE_ID=cpe-001
CPE_DEVICE_ID=dev-cpe-001
CPE_MANUFACTURER=TP-Link
CPE_MODEL=Archer C7

# ========== ACS服务器配置 ==========
ACS_WS_URL=ws://localhost:7547
ACS_IP=localhost
ACS_UDP_PORT=7548

# ========== CPE UDP配置 ==========
CPE_UDP_PORT=7548  # CPE监听的UDP端口
CPE_IP=127.0.0.1   # CPE的IP地址（用于接收UDP唤醒包）

# ========== 心跳配置 ==========
# HEARTBEAT_INTERVAL=1800          # 30分钟心跳（测试时可设为300=5分钟）
HEARTBEAT_INTERVAL=600

# ========== 连接管理配置 ==========
CPE_INACTIVITY_TIMEOUT=30000  # 连接空闲超时时间（毫秒），默认30秒
RECONNECT_INTERVAL=5000  # 重连间隔（毫秒）
MAX_RECONNECT_ATTEMPTS=10  # 最大重连尝试次数

# ========== 设备能力 ==========
CPE_CAPABILITIES=wifi,lan,wan,dhcp

# ========== 模拟配置 ==========
SIMULATE_METRICS=true
